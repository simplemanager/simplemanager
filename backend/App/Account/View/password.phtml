<?php 

$content = H::get('form');
if (!H::get('form')->isPosted()) {
    $content .= H::html(H::link(__("Retour à la page d'identification"), 'account', 'login'), 'p')->escape(false)->addCssClass('text-center');
}
echo H::html($content, 'div', ['class' => 'center-block', 'style' => 'max-width: 500px'], false);

